<a href="#" class="btn btn-success btn-ghost create" onclick={createNote()}> Create</a>
<section class="notes">
  <% if (notes && notes.length > 0) { %> <% notes.forEach(note => { %>
  <div class="dark-grey card">
    <h4 class="dark-grey card-header"><%= note.title %></h4>
    <div class=" dark-grey card-content">
      <div class="inner">
        <div class="body">
  <%= note.body%>
        </div>
        <div class="buttons">
          <a href="/notes/<%=note.id%>/edit" class="edit btn btn-primary"
            >Edit</a
          >
          <a name="<%=note.id%>" href="#" class="delete btn btn-error"
            >Delete</a
          >
        </div>
      </div>
    </div>
    <div class="footer card-header"><%= note.created_at %></div>
  </div>
  <% }) %><% } else { %>
  <div>
    <div class="alert alert-warning dark-grey empty-notes">No notes yet 😢 </div>
  </div>
  <% } %>
</section>
<div class="modal micromodal-slide" id="modal-delete"  aria-hidden="true">
  <div class="modal__overlay" tabindex="-1" data-micromodal-close>
    <div
      class="modal__container"
      role="dialog"
      aria-modal="true"
      aria-labelledby="modal-1-title"
    >
      <header class="modal__header">
        <h2 class="modal__title" id="modal-1-title">
          Confirmation
        </h2>
        <button
          class="modal__close"
          aria-label="Close modal"
          data-micromodal-close
        ></button>
      </header>
      <main class="modal__content" id="modal-1-content">
        <h4>Do you want to delete this note ?</h4>
      </main>
      <footer class="modal__footer">
        <button
          id="confirm-delete"
          class="btn btn-error"
          onclick=""
          data-micromodal-close
        >
          Continue
        </button>
      </footer>
    </div>
  </div>
</div>
